name: Hexo Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Build with Theme Fix
        run: |
          # 清理
          rm -rf site
          
          # 初始化
          npx hexo-cli@4.3.2 init site --theme landscape
          cd site
          
          # 安装所有必要依赖
          npm install hexo@8.1.1
          npm install hexo-theme-landscape
          npm install hexo-renderer-pug hexo-renderer-stylus
          
          # 🎯 强制主题设置
          echo "=== 主题强制设置 ==="
          
          # 1. 删除并重新克隆主题
          rm -rf themes/landscape
          git clone --depth=1 https://github.com/hexojs/hexo-theme-landscape.git themes/landscape
          
          # 2. 完全重建配置文件
          cat > _config.yml << 'EOF'
title: 我的博客
subtitle: ''
description: ''
keywords:
author: liudungen
language: zh-CN
timezone: Asia/Shanghai
url: https://liudungen.github.io

# URL 设置
permalink: :year/:month/:title/
pretty_urls:
  trailing_index: true
  trailing_html: true

# 主题（强制设置）
theme: landscape

# 新文章名称
new_post_name: :title.md

# 生成器
index_generator:
  per_page: 10
  order_by: -date
EOF
          
          echo "✅ 配置文件已重建"
          
          # 3. 验证主题结构
          echo "=== 主题结构 ==="
          ls -la themes/landscape/
          echo "CSS文件:"
          find themes/landscape -name "*.styl" -o -name "*.css" | head -10
          
          # 处理文章
          rm -f source/_posts/hello-world.md
          mkdir -p source/_posts
          [ -d "../source/_posts" ] && cp -r ../source/_posts/* source/_posts/ 2>/dev/null || true
          
          # 生成
          echo "=== 生成静态文件 ==="
          npx hexo clean
          npx hexo generate
          
          # 4. 验证生成结果
          echo "=== 验证生成 ==="
          echo "CSS文件:"
          ls -la public/css/ 2>/dev/null || echo "无css目录"
          
          echo "首页样式链接:"
          grep -o 'href="[^"]*\.css"' public/index.html 2>/dev/null || echo "无CSS链接"
          
          # 部署
          cd public
          git config --global user.name "liudungen"
          git config --global user.email "798695922@qq.com"
          git init
          git checkout -b main
          git add -A
          git commit -m "Deploy with theme fix $(date +'%Y-%m-%d %H:%M:%S')"
          git push -f "https://liudungen:${{ secrets.HEXO_DEPLOY_TOKEN }}@github.com/liudungen/liudungen.github.io.git" main
          
          echo "🎉 部署完成！强制刷新访问：https://liudungen.github.io?t=$(date +%s)"
