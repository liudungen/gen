name: Hexo Deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build and Deploy
        run: |
          # 清理
          rm -rf blog
          
          # 初始化
          npx hexo-cli init blog
          cd blog
          
          # 安装
          npm install hexo@8.1.1 hexo-theme-butterfly
          
          # 🎯 直接使用所有配置
          echo "=== 复制配置文件 ==="
          cp ../_config.yml . 2>/dev/null || true
          cp ../_config.butterfly.yml . 2>/dev/null || true
          
          # 🎯 确保使用 butterfly 主题
          sed -i 's|^theme:.*|theme: butterfly|' _config.yml 2>/dev/null || echo "theme: butterfly" >> _config.yml
          
          # 🎯 复制整个主题目录（包含图片）
          if [ -d "../themes" ]; then
            echo "✅ 复制 themes 目录"
            mkdir -p themes
            cp -r ../themes/* themes/ 2>/dev/null || true
          fi
          
          # 🎯 复制 source 目录
          if [ -d "../source" ]; then
            echo "✅ 复制 source 目录"
            cp -r ../source/* source/ 2>/dev/null || true
          fi
          
          # 🎯 验证图片
          echo "=== 图片验证 ==="
          if [ -f "themes/butterfly/source/img/index_bg.png" ]; then
            echo "✅ banner 图存在: themes/butterfly/source/img/index_bg.png"
            ls -la themes/butterfly/source/img/
          else
            echo "❌ banner 图不存在"
            find . -name "index_bg.png" -o -name "*banner*" | head -10
          fi
          
          # 生成
          npx hexo clean
          npx hexo generate
          
          # 部署
          cd public
          git config --global user.name "liudungen"
          git config --global user.email "798695922@qq.com"
          git init
          git checkout -b main
          git add -A
          git commit -m "Deploy with banner image $(date +'%Y-%m-%d %H:%M:%S')"
          git push -f "https://liudungen:${{ secrets.HEXO_DEPLOY_TOKEN }}@github.com/liudungen/liudungen.github.io.git" main
          
          echo "🎨 部署完成！banner图应该显示了。"
