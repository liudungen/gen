name: Hexo Deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build and Deploy
        run: |
          # 清理
          rm -rf blog
          
          # 初始化
          npx hexo-cli init blog
          cd blog
          
          # 安装依赖
          npm install hexo@8.1.1 hexo-renderer-pug hexo-renderer-stylus
          npm install hexo-theme-butterfly  # 🎯 安装蝴蝶主题
          
          # 🎯 配置蝴蝶主题
          sed -i 's|^theme:.*|theme: butterfly|' _config.yml
          sed -i 's|^permalink:.*|permalink: :year/:month/:title/|' _config.yml
          
          # 如果有自定义配置，使用它
          if [ -f "../_config.yml" ]; then
            echo "使用你的配置..."
            cp ../_config.yml _config.yml
            # 确保主题正确
            sed -i 's|^theme:.*|theme: butterfly|' _config.yml
          fi
          
          # 如果有主题配置文件，也复制
          if [ -f "../_config.butterfly.yml" ] || [ -f "../_config.butterfly.yaml" ]; then
            echo "复制主题配置..."
            cp ../_config.butterfly.yml _config.butterfly.yml 2>/dev/null || true
            cp ../_config.butterfly.yaml _config.butterfly.yaml 2>/dev/null || true
          fi
          
          # 删除默认文章
          rm -f source/_posts/hello-world.md
          
          # 复制文章
          mkdir -p source/_posts
          if [ -d "../source/_posts" ]; then
            cp -r ../source/_posts/* source/_posts/ 2>/dev/null || true
          fi
          
          # 生成
          npx hexo clean
          npx hexo generate
          
          # 验证
          if [ ! -d "public" ]; then
            echo "生成失败"
            exit 1
          fi
          
          # 部署
          cd public
          git config --global user.name "liudungen"
          git config --global user.email "798695922@qq.com"
          git init
          git checkout -b main
          git add -A
          git commit -m "Deploy with butterfly $(date +'%Y-%m-%d %H:%M:%S')"
          git push -f "https://liudungen:${{ secrets.HEXO_DEPLOY_TOKEN }}@github.com/liudungen/liudungen.github.io.git" main
          
          echo "🎉 蝴蝶主题部署完成！访问：https://liudungen.github.io"
